services:
  db:
    image: postgres:18-alpine
    container_name: db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: prit-is-the-skit
      POSTGRES_DB: prit
    ports:
      - "5432:5432"
